const express = require('express');

const app = express();

console.log('\n 1. run "npm init --yes" in root to generate the package.json file');
console.log('\n 2. then install required dependencies');

console.log('\n 3. please check package.json for project related configurations and dependency management');
console.log('\n 4.run "npm list" to see all the dependencies');
console.log('\n 5.run "npm list --depth=0" to see all the main dependencies');